/*:
 
 Here we have a struct called `Person`. If we think about protocols as a sort of persona that this person can adopt, an example might be a cook, a developer, etc.
 
 Create a protocol called `Soldier`. What sort of attributes or things does a soldier do? Create at least one property and one function in this protocol that would make sense for a soldier.
 
 */

struct Person {
    let name: String
    let age: String
}

//: [Next](@next)
